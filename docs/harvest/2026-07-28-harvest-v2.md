# CoupleOS → AppSeed Hasat v2 (2026-07-28)

**Bağlam:** v1 hasadı (`docs/harvest/2026-07-25-coupleos-harvest.md`) Faz 0-6 kapandı, `harvest/coupleos-v1`
branch'i `main`'e merge edilmeyi bekliyor. Baseline build 2026-07-28'de yeniden alındı: **exit 0, own-code
warning yok.**

**v2'nin amacı:** v1'de bilinçli olarak kapsam dışı bırakılan ama aslında *generic* olan birikimi almak +
26 Temmuz sonrası CoupleOS değişikliklerini senkronlamak + seed'i agentic kullanıma açmak.

**Kural seti (v1'den devam):**
- CoupleOS salt-okunur, sıfır dokunuş.
- Domain sızıntısı sıfır: taşınan her parça `Base/`, `Helpers/`, `Extensions/` sözleşmesine uyar, isim ve
  API'sinden CoupleOS domain'i okunmaz.
- Her faz sonunda review kapısı: gerçek diff okuması + build exit 0.
- Döngü: **plan → review → taşı → review**.

## Tespit: v1 sonrası durum

| Alan | Durum |
|---|---|
| `Extensions/` (22 dosya) | ✅ Senkron. Tek fark `URL+Extension`: `QueryParameters` seed'de Network katmanında — bilinçli. `UIColor.isLight` seed'de fazladan var. |
| `Base/UI/` | ✅ Seed CoupleOS'un üstünde (RingProgress, Tooltip, ExpandableAddField, LockedOverlay, PillSearchField, PhotoViewerCell, LargeTitleSectionHeader, PaperBackgroundView, FloatingWidgetView). |
| `Base/UI/Form/` (12) | ✅ Birebir. |
| `Helpers/` | ✅ Çekirdek senkron. |
| Widget target | ✅ `WidgetHelpers`/`WidgetColorKit` birebir hizada. |
| **Boşluk 1 — drift** | `ReviewPromptManager` 26 Temmuz'da sadeleşti (magic-moment düştü, tek-sor kaldı); seed eski mantıkta. |
| **Boşluk 2 — Map kit** | MapKit annotation ailesi (foto-yığın pin, chip pin, cluster rozeti, avatar pin) hiç hasat edilmedi. |
| **Boşluk 3 — carousel** | Sayfalı carousel + page control (`DrawingCarouselView`) hasat edilmedi. |
| **Boşluk 4 — local reminder** | `DateRemindersHelper`/`MissionRemindersHelper` — prefix-scoped "hepsini iptal et, listeden yeniden kur" kalıbı; seed'de sadece demo scheduler var. |
| **Boşluk 5 — generic scene** | `ReviewPromptSheet`, `PermissionSheet`, `QRScanner`, `PhotoViewer` scene'leri seed'de yok. |
| **Boşluk 6 — Share extension** | v1 Faz 5'te "şablon" denmişti; repoda karşılığı yok. |
| **Boşluk 7 — backend tohumları** | Seed'de yalnız `send-push` + 2 SQL şablonu var. CoupleOS'ta üretimde kanıtlanmış: feedback fn, RevenueCat webhook, storage RLS kilidi, yetim dosya süpürme + purge kuyruğu, `app_config` admin paneli, SQL regresyon test koşumu. |
| **Boşluk 8 — agentic katman** | Seed'de `.claude/` yok. Xcode file template'leri var ama bir agent onları kullanamaz — CLI üretici yok. |

## Fazlar

### F1 — Drift senkronu ✅ (2026-07-29)
- `ReviewPromptManager` + `UserDefaultsWrapper`: CoupleOS'un son hâli (tek sor, N açılış sonrası), seed'de
  pair yerine generic "aktivasyon" kavramıyla.
- `WidgetSyncService`: CoupleOS'ta `pairId → scopeId` dersi; seed karşılığı zaten scope-agnostik, yalnız
  doğrulanır.
- Kapı: diff okuması + build.

### F2 — Base UI terfileri ✅ (2026-07-29)
- `Base/UI/Map/`: `PhotoStackAnnotation(+View)`, `LabelPinAnnotation(+View)`, `AvatarAnnotation(+View)`,
  `ClusterCountAnnotationView`. Domain modeli yok — hepsi id + görsel/metin alır.
- `Base/UI/CollectionView/PagedCarouselView` + `CarouselPageCell`: sayfalı yatay carousel + page control.
- `Base/UI/UIView/StatusBubbleView`: app tarafı durum baloncuğu (widget'ta `StatusBubbleShape` zaten var).
- Kapı: her bileşen tek tek okunur (leak taraması), build.

### F3 — Helper terfileri ✅ (2026-07-29)
- `LocalReminderHelper`: prefix-scoped senkron (`sync(prefix:reminders:)`), per-item saat, iptal, izin
  kontrolü. `NotificationHelper`'ın demo scheduler'ı bunun üstüne taşınır.
- Kapı: build + `NotificationHelper` ile çakışma kontrolü.

### F4 — Generic scene'ler ✅ (2026-07-29)
- `ReviewPromptSheet` (kendi sorusu → `SKStoreReviewController`), `PermissionSheet` (izin priming),
  `QRScanner` (kamera + kod okuma), `PhotoViewer` (zoom transition + `PhotoViewerCell`).
- Hepsi seed sözleşmesine uygun 4 dosya + `Localizable` + router bağlantısı.
- Kapı: derleme + l10n parite script'i (sim görsel testi yapılmadı).

### F5 — Extension target + backend tohumları ✅ (2026-07-29)
- `AppSeedShareExtension` iskeleti: App Group üzerinden ana app'e devir + parser şablonu.
- `supabase/templates/sql/`: storage RLS kilidi, yetim süpürme + purge kuyruğu, `app_config` + `admins` +
  `is_admin()`, `delete_my_account`.
- `supabase/functions/`: `feedback` (generic — Telegram'dan arındırılmış webhook), `revenuecat-webhook`.
- `supabase/tests/`: SQL regresyon koşum şablonu.
- Kapı: secret-scan + 3 target build.

### F6 — Agentic katman ⏸ ERTELENDİ (ayrı iş, Tunay kararı)
- `.claude/settings.json` + `.claude/skills/`: `new-scene`, `new-service`, `new-widget-kind`,
  `db-migration`, `l10n-key`, `release`.
- `Scripts/seed-scene.swift` (ve kardeşleri): CLI üretici. Klasör-senkron gruplar sayesinde pbxproj'e
  dokunmaya gerek yok.
- `CLAUDE.md`'ye "Agent ile çalışma" bölümü + docs güncellemesi.
- Kapı: üretilen dosyalar derlenir, kontrat kontrolü.

### F7 — Final review ✅ (2026-07-29)
Clean build exit 0 (4 target, sıfır own-code warning), Renamer smoke geçti, doküman turu
tamam. Rapor: `docs/harvest/2026-07-29-harvest-v2-review.md`.

## Kapsam dışı (v1 kararı korunur)
Pair/premium domain'i, domain scene'ler ve widget'ları, Weather üçlüsü, LocationHelper, SQL migration
geçmişi. Ek olarak v2'de değerlendirilip **elenenler**: `PaperView`/`DrawingToolbar` (çizim motoru — tek
başına bir ürün kararı, seed'e ağır), `CalendarMonthCell` (ay ızgarası domain kurallarına gömülü).
