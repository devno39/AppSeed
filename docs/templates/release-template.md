# vX.Y.Z — <one-line summary> (shipped YYYY-MM-DD)

> Per-version shipped notes. Save as `docs/releases/vX.Y.Z.md`. Write it in the
> same commit that ships the version.

Status: <live on the App Store (Build NN, submitted from `release/X.Y.Z`) | in review | TestFlight>.
<Any same-day post-release action, e.g. prod `app_config.minimum_supported_version` raised A → B.>

## Shipped in this version

- **<Feature area>** — what shipped, in the user's terms, with the load-bearing
  technical facts (migration numbers, new tables/widgets/kinds, limits). One
  bullet per coherent feature; nest specifics inline rather than as sub-bullets.
- **<Feature area>** — …
- **Standards sweep** — cross-cutting cleanups folded into this release
  (protocol triples, MARK order, weak-self bindings, l10n fixes).

## Post-release notes

- Known issues discovered after submission and where they're fixed (next version).
- Anything deferred out of this build and why (frozen at Build NN, moved to `release/X.Y.(Z+1)`).
